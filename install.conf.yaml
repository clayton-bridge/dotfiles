- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/fish/config.fish:
        create: true
        path: fish/config.fish
        force: true
#    ~/.gitconfig:
#        creat: true
#        path: ~/.dotfiles/git/gitconfig

- shell:
  - gitname